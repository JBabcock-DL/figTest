# Plan (WO-001)

## Notes

- Research captured in `research/signup-form-implementation.md`:
  - Prefer `src/components/ui/form.tsx` wrappers for accessible form wiring.
  - Use `src/styles/tokens.css` CSS custom properties directly (Tailwind arbitrary values) to match Figma spacing/radius/borders/typography.
  - No sign-in route currently exists in `src/app/*`; confirm the intended path for the footer link.

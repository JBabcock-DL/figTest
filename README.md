# figTest

Scratch repo for exercising `/create-design-system` Step 13b — generated tokens live at `src/styles/tokens.css`. Point your app or a static HTML file at that stylesheet to verify imports and theme/font-scale attributes.
